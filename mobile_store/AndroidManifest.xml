<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.gopro.mobile_store"
    android:versionCode="1"
    android:versionName="0.0.1-SNAPSHOT" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="13" />

    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/MobileStoreTheme" >
        
        <activity android:name=".ui.HomeScreenActivity"
            android:label="@string/home_title"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.LoginActivity" 
         	android:label="@string/login_title" 
         	android:exported="false" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>       
        <activity android:name=".ui.ActionMenuActivity" />
        
         <provider 
            android:name="rs.gopro.mobile_store.provider.MobileStoreContentProvider"
            android:authorities="rs.gopro.mobile_store.provider.mobile_store_provider"></provider>
        
        
    </application>

</manifest>